import { create } from 'zustand';

<% if (language === 'ts') { %>
interface State {
  count: number;
  inc: () => void;
}
<% } %>

export const useStore = create<% if (language === 'ts') { %><State><% } %>((set) => ({
  count: 0,
  inc: () => set((state) => ({ count: state.count + 1 })),
}));
